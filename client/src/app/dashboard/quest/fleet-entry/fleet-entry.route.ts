import { Route } from '@angular/router';

import { FleetEntryComponent } from './index';

export const FleetEntryRoutes: Route[] = [
  {
    path: 'fleetentry',
    component: FleetEntryComponent
  }
];
